name: IdFit
options:
  bundleIdPrefix: net.shershnev
  deploymentTarget:
    macOS: "15.0"
  createIntermediateGroups: true
settings:
  base:
    SWIFT_VERSION: "6.0"
    CODE_SIGN_IDENTITY: "-"
    CODE_SIGN_STYLE: Manual
targets:
  IdFit:
    type: application
    platform: macOS
    sources:
      - path: Sources/IdFit
        excludes: [Resources]
      - path: Sources/IdFit/Resources/AppIcon.icns
        buildPhase: resources
    info:
      path: Sources/IdFit/Info.plist
      properties:
        CFBundleName: ID Fit
        CFBundleDisplayName: ID Fit
        LSMinimumSystemVersion: "15.0"
        NSHighResolutionCapable: true
        CFBundleIconFile: AppIcon
        CFBundleShortVersionString: "1.0"
        CFBundleVersion: "1"
        NSHumanReadableCopyright: ""
        # Lets Finder offer ID Fit under "Open With" for folders, and lets the
        # command line tool hand a folder over with `open -b`.
        UTExportedTypeDeclarations:
          - UTTypeIdentifier: net.shershnev.id-fit.document
            UTTypeDescription: ID Fit Document
            UTTypeConformsTo: [public.json]
            UTTypeTagSpecification:
              public.filename-extension: [idfit]
        CFBundleDocumentTypes:
          # Clicking the document opens the folder holding it.
          - CFBundleTypeName: ID Fit Document
            CFBundleTypeRole: Editor
            LSHandlerRank: Owner
            LSItemContentTypes: [net.shershnev.id-fit.document]
          - CFBundleTypeName: Folder of Scans
            CFBundleTypeRole: Viewer
            LSHandlerRank: Alternate
            LSItemContentTypes: [public.folder]
          # Finder offers no "Open With" on a folder, only on files — so
          # scans are accepted too and open the folder holding them. Rank
          # "Alternate" keeps ID Fit from ever becoming the default viewer.
          - CFBundleTypeName: Scan
            CFBundleTypeRole: Viewer
            LSHandlerRank: Alternate
            LSItemContentTypes: [public.image, com.adobe.pdf]
        NSServices:
          # Accepts scans as well as folders: inside an open folder there is
          # nothing to right-click but the files, and any one of them
          # identifies the folder to open.
          - NSMenuItem:
              default: Open Folder in ID Fit
            NSMessage: openFolder
            NSPortName: ID Fit
            NSSendFileTypes: [public.folder, public.image, com.adobe.pdf]
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: net.shershnev.id-fit
        ENABLE_HARDENED_RUNTIME: NO
  IdFitTests:
    type: bundle.unit-test
    platform: macOS
    sources: [Tests/IdFitTests]
    settings:
      base:
        GENERATE_INFOPLIST_FILE: YES
    dependencies:
      - target: IdFit
schemes:
  IdFit:
    build:
      targets:
        IdFit: all
    run:
      config: Debug
    test:
      config: Debug
      targets: [IdFitTests]
